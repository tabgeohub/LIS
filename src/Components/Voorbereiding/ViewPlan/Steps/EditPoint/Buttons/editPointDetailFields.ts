/** Shared editable detail fields for ViewPlan EditPoint Buttons/Submit. */
export type EditPointDetailFields = {
  omschrijving: string;
  activiteit: string;
  organisatie: string;
  specifiekLettenOp: string;
};
