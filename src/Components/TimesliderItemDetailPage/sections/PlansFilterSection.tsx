/** Flight plan selection / filters — content TBD. */
export default function PlansFilterSection() {
  return (
    <section
      className="flex h-12 shrink-0 items-center border-b border-gray-200 bg-gray-50 px-4"
      aria-label="Plannen filter"
    >
      <span className="text-xs text-gray-500">Plans filter</span>
    </section>
  );
}
