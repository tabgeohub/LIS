/** Large primary image / viewer — content TBD. */
export default function MainImageSection() {
  return (
    <section
      className="flex min-h-0 flex-1 flex-col bg-gray-900/5"
      aria-label="Hoofdafbeelding"
    >
      <div className="flex min-h-0 flex-1 items-center justify-center border border-dashed border-gray-300 bg-white/80 m-2 rounded-md">
        <span className="text-sm text-gray-400">Main image</span>
      </div>
    </section>
  );
}
