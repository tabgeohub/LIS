export type IdNaam = { id: string; naam: string };
export type IdActiviteit = { id: string; activiteit: string };
export type Regio = {
  id: string;
  naam: string;
  shape_Length: number;
  shape_Area: number;
};
